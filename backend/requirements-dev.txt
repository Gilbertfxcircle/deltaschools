# ============================================================================
# Delta Plax Education Suite - LOCAL DEV dependencies (SQLite, no Postgres/queue)
# ----------------------------------------------------------------------------
# Use this for running locally on your own machine:
#     pip install -r requirements-dev.txt
#
# Differences from requirements.txt (the full cloud stack):
#   * No psycopg (PostgreSQL), celery, redis, or minio  -> not needed for a
#     local SQLite run, and these are what fail to install on the newest Python.
#   * Version ranges (>=) instead of hard pins, so pip can pick wheels that
#     exist for YOUR Python version (incl. Python 3.13 / 3.14).
#
# Recommended: use Python 3.11 or 3.12 for the smoothest experience.
# ============================================================================

# --- Web framework / ASGI ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.17
websockets>=13

# --- Settings / validation ---
pydantic>=2.9
pydantic-settings>=2.6
email-validator>=2.2

# --- Database / ORM / migrations (SQLite uses the stdlib driver) ---
SQLAlchemy>=2.0.36
alembic>=1.14

# --- Security / auth ---
passlib[bcrypt]>=1.7.4
bcrypt>=4.2
pyjwt>=2.10
pyotp>=2.9
cryptography>=43

# --- HTTP client (sync engine, AI, SMS) ---
httpx>=0.27

# --- Testing ---
pytest>=8.3
pytest-asyncio>=0.24
