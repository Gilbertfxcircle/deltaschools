# ============================================================================
# Delta Plax Education Suite - Backend (FastAPI) dependencies
# Stack pinned per the developer brief. Python 3.11+.
# ============================================================================

# --- Web framework / ASGI ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
websockets==13.1

# --- Settings / validation ---
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Database / ORM / migrations ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3        # PostgreSQL 15 (cloud)
# SQLite uses the stdlib driver via "sqlite+pysqlite" (no extra dep)

# --- Security / auth ---
passlib[bcrypt]==1.7.4        # bcrypt password hashing (cost factor >= 12)
bcrypt==4.2.0
pyjwt==2.10.0                 # JWT access/refresh tokens
pyotp==2.9.0                  # TOTP two-factor auth
cryptography==43.0.3          # AES-256 field encryption at rest

# --- Task queue ---
celery==5.4.0
redis==5.2.0

# --- Object storage ---
minio==7.2.10

# --- HTTP client (sync engine, AI, SMS, payment gateways) ---
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
